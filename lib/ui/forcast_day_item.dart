import 'package:flutter/material.dart';

class ForcastDayItem extends StatelessWidget {
  const ForcastDayItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.zero,
      child: const Column(
        children: [
          Text("forcast item here"),
        ],
      ),
    );
  }
}
